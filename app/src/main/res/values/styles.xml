<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccentOrange</item>
    </style>

    <style name="ListasTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccentOrange</item>
    </style>

    <style name="buttonStyle">
        <item name="android:background">@color/whiteColor</item>
        <item name="android:shadowRadius">0</item>
        <item name="android:padding">5dp</item>
        <item name="android:fontFamily">@font/rock_salt</item>
        <item name="android:textSize">10dp</item>
    </style>

    <style name="buttonBorderStyle">
        <item name="android:background">@color/colorAccentRed</item>
        <item name="android:textColor">@color/colorAccentRed</item>
        <item name="android:paddingBottom">5dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">5dp</item>
        <item name="android:layout_marginBottom">15dp</item>
    </style>
</resources>
