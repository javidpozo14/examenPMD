<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cenec.examen16diciembre">

    <application
        android:allowBackup="true"
        android:icon="@drawable/candoit"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconoapp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Lista"
            android:theme="@style/ListasTheme"
            ></activity>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Diseño"></activity>
        <activity android:name=".Preferencias"></activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>